/**
 * PWA-S12 (#464) — `signout` module API : pure helpers shared between the
 * `/api/signout` route handler and its tests.
 */
export {
  CONVEX_AUTH_COOKIE_BASE_NAMES,
  convexAuthCookieNames,
  isLocalHost,
} from "./convex-auth-cookies";
